# Rate-limit zone for profit-partner-path /api/ endpoints (lead capture + webhooks).
# Lives in the http{} context via conf.d include. Unique name avoids clashing with other sites.
limit_req_zone $binary_remote_addr zone=ppp_api:10m rate=10r/s;
